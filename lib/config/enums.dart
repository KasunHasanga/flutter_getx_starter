

enum AccountType { personal, business }

enum TextFieldType { creditCard, ccDate, normal }

enum AddMoneyType { creditCard, applePay, transfer }

enum SpacesTypes {
  generalSavings,
  debtPayOff,
  trip,
  emergency,
  investment,
  retirement
}

enum TransactionPeriodType {
  today,
  thisWeek,
  lastWeek,
  thisMonth,
  lastMonth,
  custom,
}
